import 'package:flutter/material.dart';

class NavBarCollapsed extends StatelessWidget {
  const NavBarCollapsed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
